<form method="post" action="{{ path('app_prescription_line_delete', {'id': prescription_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescription_line.id) }}">
    <button class="btn">Delete</button>
</form>
